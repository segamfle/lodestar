node_modules/
dist/
.env
.env.*
vendor/casino-sdk.zip
vendor/casino-sdk/
superteam_cache/
*.log
package-lock.json
config/payout-addresses.json
games/*/dist/
cookiechain/*/dist/

# Unrelated work that happens to live in the same folder. `git add -A` swept seven files of
# it - including a compiled binary - into a public repository once. Not again.
trader/
